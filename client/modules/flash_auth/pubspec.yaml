name: flash_auth
description: "Flash IM 认证模块 — 登录、验证码、登录策略"
version: 0.0.2
homepage:

environment:
  sdk: ^3.11.1
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter
  dio: ^5.8.0+1
  oktoast: ^3.4.0
  shared_preferences: ^2.3.0
  flutter_bloc: ^8.1.0
  equatable: ^2.0.0
  go_router: ^17.1.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
