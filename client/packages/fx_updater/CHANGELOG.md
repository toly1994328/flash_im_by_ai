## 0.1.0

- 首次发布
- 版本号模型（AppVersion），支持语义化版本比较
- 更新检查器（UpdateChecker），通过回调注入数据获取
- 更新弹窗（UpdateDialog），支持下载进度、强制更新、重试
- 平台策略接口：Android、桌面端、macOS、iOS/URL 跳转、空策略
- 全局状态管理器（FxUpdater 单例）
- 红点组件（UpdateBadge）
