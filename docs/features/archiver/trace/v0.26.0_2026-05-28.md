# v0.26.0 存档快照

- 日期：2026-05-28
- 节点总数：143
- 变更摘要：macOS 桌面端构建与 UI 适配 + verify_codes 多行存储修复

---

## 本次新增节点

| 编号 | 功能节点 | 层级 | 模块 | 简介 |
|------|---------|------|------|------|
| P-64 | 桌面端自适应布局 | 前端业务层 | home (desktop_layout + mobile_layout) | 根据窗口宽度切换桌面端分栏/移动端底部导航布局 |
| P-65 | 桌面端会话分栏 | 前端业务层 | home (desktop_layout) + flash_im_chat | 桌面端下消息 Tab 三栏展示，通讯录/设置两栏展示 |

---

## 本次变更的局域网络

### 新增：桌面端 UI（modules/desktop/client.md）

桌面端自适应布局与分栏交互。

### 变更：Auth 域（后端内部优化）

- `verify_codes` 表从主键约束改为自增 id + 索引，支持多行存储保留历史记录
- `send_sms` / `send_email_code` 从 UPSERT 改为纯 INSERT
- 校验逻辑改为取最新一条 `status = 0` 的记录，按 id 精确标记已使用

---

## 运维流程（不分配编号）

- macOS 构建脚本 `scripts/build_center/build_macos.py`：flutter build → codesign → DMG 打包 → 公证 → App Store 上传
- 窗口管理集成（window_manager）：最小尺寸、标题栏隐藏、拖拽区域
- IM 调色主题（FlashImTheme ThemeExtension）
