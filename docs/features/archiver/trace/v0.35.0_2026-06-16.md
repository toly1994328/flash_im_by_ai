# v0.35.0 存档快照

日期：2026-06-16
类型：功能迭代（图片/视频预览体验升级）
节点总数：156（不变，体验优化无新功能节点）

---

## 变更摘要

用 tolyui_mediax 库重做图片展示和预览体验，同时修复视频发送/播放问题。

---

## 前端变更

| 变更 | 说明 |
|------|------|
| MediaSourceProvider 注入 | App 顶层注入 CachedNetworkImage Resolver + 持久化缓存 |
| ImageBubble 改造 | MediaImageView + Hero 动画 + 缩略图级别 + 淡边框 |
| BubbleSize 尺寸策略 | 独立文件，250×300/min80/极端裁剪 |
| MediaPreviewPage 预览 | 多图滑动 + Hero 过渡 + FadeTransition |
| 本地文件优先 | 有 localData.path 时 FileSource 跳过网络 |
| 发送前读取图片宽高 | 气泡尺寸立即正确 |
| content 上传后更新 | 图片/视频 content 从本地路径更新为服务器 URL |
| VideoBubble 优化 | BubbleSize + 淡边框 + 封面缓存 + 播放/下载图标 |
| 视频 localData 分离 | path(视频) + thumbnail_path(封面) 两字段 |
| 删除 ImagePreviewPage | 被 MediaPreviewPage 替代 |

## 后端变更

| 变更 | 说明 |
|------|------|
| 缩略图上限 200→400px | 2x 屏气泡清晰度提升 |

## 工具变更

| 变更 | 说明 |
|------|------|
| fix_sqlite3_cache.py | sqlite3 build hook 本地缓存修复脚本 |
| flutter-mediax-usage Skill | tolyui_mediax 使用指南技能 |
