# v0.34.0 存档快照

日期：2026-06-14
类型：代码体检（重构治理）
节点总数：不变（无新增功能节点）

---

## 变更摘要

本版为客户端大文件治理，不涉及新功能开发。对两个超标文件进行架构级重构，新增基础设施支撑后续迭代。

---

## 重构变更

### flash_im_chat 模块

| 变更 | 说明 |
|------|------|
| chat_page.dart | 987 行 → 331 有效行，纯 UI 组装层 |
| 新增 ChatMediaHandler | 媒体文件操作处理器 |
| 新增 ChatMenuHandler | 菜单事件分发处理器 |
| 新增 ChatGroupCubit | 群组状态管理（从 View 层下沉） |
| 新增 PeerStatusCubit | 对端在线状态管理（从 View 层下沉） |
| 新增 ChatTarget / ChatViewOptions | 参数值对象 |
| 新增 ShowToastEvent | 全局 toast 事件（替代 try-catch 包裹） |
| 新增 MentionMember（data 层） | 从 view 层下沉 |
| 新增 Message.contentSummary | Extension 封装类型摘要 |
| view 目录结构化 | 10 个功能子文件夹（index/bubble/input/media/menu/notice/pinned/picker/info/voice_input） |

### flash_im_group 模块

| 变更 | 说明 |
|------|------|
| group_chat_info_page.dart | 881 行 → 337 有效行，StatelessWidget |
| 新增 GroupDetail / GroupMember | 强类型数据类（替代 Map<String, dynamic>） |
| 新增 GroupInfoCubit | 群详情状态管理 + 所有操作 |
| 新增 GroupInfoScope | Cubit 注入与组件解耦 |
| 新增 GroupMemberGrid | 成员网格独立组件 |
| 新增 GroupSettingWidgets | 通用设置项组件（SettingItem/SwitchItem） |
| 新增 GroupActionSection | 底部操作按钮 |
| 新增 DashedBorderPainter | 虚线边框绘制器 |

### 技能 & 规范

| 变更 | 说明 |
|------|------|
| 新增 widget-code-review Skill | 组件评审技能 |
| 新增 widget-refactor Skill | 组件重构技能 |
| 更新 code-style steering | 注释规范（类顶部 `///`，方法体内不写） |
| 更新 code-style steering | 文件行数标准 500 行 |

---

## 代码健康度

全部业务文件有效代码 ≤ 500 行，`dart analyze lib` 零错误。
