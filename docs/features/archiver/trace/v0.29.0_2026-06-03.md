# v0.29.0 存档快照

- 日期：2026-06-03
- 节点总数：151（无新增功能节点）
- 变更摘要：Web 加载优化（基础设施迭代，非业务功能）

---

## 本次变更

本次为基础设施优化迭代，不涉及新的业务功能节点。

### 后端变更（server v0.1.6）

| 文件 | 变更 |
|------|------|
| `Cargo.toml` | tower-http features 新增 `compression-gzip`, `compression-br`, `set-header`；版本 0.1.5 → 0.1.6 |
| `src/main.rs` | 新增 CompressionLayer（Gzip/Brotli 压缩）+ COOP/COEP 响应头（启用 WASM 多线程）；静态目录 `static/web` → `static/im` |

### 前端变更（client）

| 文件 | 变更 |
|------|------|
| `web/index.html` | 重写，引用 loading/ 下的独立文件 |
| `web/loading/loading.css` | 新增，加载页样式（粒子网络 + 旋转光环 + 进度条 + 底部装饰线） |
| `web/loading/loading.js` | 新增，粒子动画 + flutter-first-frame 退出逻辑 |
| `modules/flash_starter/lib/src/splash_page.dart` | Web 端跳过隐私弹窗 |

### 构建脚本变更

| 文件 | 变更 |
|------|------|
| `scripts/build_center/build_web.py` | 重写：默认 WASM 模式 + `--tree-shake-icons` + 部署目录改为 `static/im` |

---

## 优化效果

| 指标 | 优化前 | 优化后 |
|------|--------|--------|
| transferred | 8.0 MB | 4.7 MB |
| Finish | 8.65 s | 3.69 s |
| 首屏白屏 | 有（等待 JS 加载） | 无（143ms 显示加载动画） |
| 代码安全性 | JS 可读 | WASM 二进制不可读 |
| 多线程渲染 | 不支持 | 支持（Chrome 119+） |

---

## 文档产出

| 文件 | 内容 |
|------|------|
| `docs/ref/doc/books/draft/web/web_loading_optimize.md` | 完整章节：Web 运行 + 部署 + 压缩 + Loading 动画 + WASM 介绍 |
| `docs/features/ui/desktop/v0.0.3/analysis.md` | 精简版需求分析 |
| `docs/features/ui/desktop/v0.0.3/server/design.md` | 后端设计（中间件配置） |
| `docs/features/ui/desktop/v0.0.3/client/design.md` | 前端设计（加载页 + 构建脚本） |
