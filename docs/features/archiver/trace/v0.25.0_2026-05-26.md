# v0.25.0 存档快照

- 日期：2026-05-26
- 节点总数：141（+6）
- 变更摘要：Apple OAuth 登录 + 邮箱验证码登录 + 登录页 Tab 切换 + verify_codes 统一验证码表

---

## 本次新增节点

| 编号 | 功能节点 | 层级 | 模块 | 简介 |
|------|---------|------|------|------|
| I-17 | Apple OAuth 登录 | 基础设施 | flash-auth (oauth/apple) | identity_token JWKS 验证 + 用户创建 |
| I-18 | 邮箱验证码发送 | 基础设施 | flash-auth (email/sender) | SMTP 发送 + verify_codes 表 + 频率限制 |
| I-19 | 邮箱登录 | 基础设施 | flash-auth (handler) | 验证码/密码双模式登录 + 自动注册 |
| P-61 | 登录页 Tab 切换 | 前端业务 | flash_auth (login_segment_tab) | iOS CupertinoSegmentedControl 风格 |
| P-62 | 邮箱登录表单 | 前端业务 | flash_auth (email_login_form) | 邮箱 + 验证码输入 + 倒计时 |
| P-63 | Apple 登录入口 | 前端业务 | flash_auth (login_page) | sign_in_with_apple 插件集成 |

---

## 数据库变更

- 新增 `verify_codes` 表（统一替代 sms_codes + email_codes）
- 字段：identifier / channel / scene / code / status / expires_at / request_ip / sender / created_at
- 主键：(identifier, channel, scene)
- 删除旧 `sms_codes` 建表语句

---

## 变更的局域网络

### Auth 域后端

- 新增 `oauth/apple.rs`：AppleProvider 实现 OAuthProvider trait
- 新增 `email/sender.rs`：SMTP 邮件发送器（lettre crate）
- 新增 `email/mod.rs`：邮件模块入口
- 修改 `handler.rs`：send_email_code + apple_login + login_with_email
- 修改 `model.rs`：LoginType::Email + AppleLoginRequest + EmailCodeRequest
- 修改 `routes.rs`：新增 /auth/apple + /auth/email/code
- 新增依赖：lettre 0.11

### Auth 域前端

- 新增 `email_login_strategy.dart`：邮箱验证码策略
- 新增 `email_login_form.dart`：邮箱 + 验证码表单
- 新增 `login_segment_tab.dart`：iOS Segment 切换组件
- 新增 `other_login_row.dart`：底部其他登录方式（拆分自 login_page）
- 修改 `login_mixin.dart`：LoginTab enum + emailStrategy + loginWithAppleToken
- 修改 `login_page.dart`：Tab 切换 + Apple 图标 + 组件化拆分
- 修改 `auth_repository.dart`：sendEmailCode + loginWithApple
- 新增依赖：sign_in_with_apple ^6.1.0
